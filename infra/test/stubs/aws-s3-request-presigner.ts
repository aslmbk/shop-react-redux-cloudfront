/**
 * Test stub for `@aws-sdk/s3-request-presigner`.
 * Exports `getSignedUrl` as a `jest.fn()` so individual tests can configure its
 * behavior directly (e.g. `mockResolvedValue`, `mockRejectedValue`).
 */
export const getSignedUrl = jest.fn();
